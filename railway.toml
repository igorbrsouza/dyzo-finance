[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "echo '=== DB push ===' && npx prisma db push && echo '=== Init DB ===' && node scripts/init-db.js && echo '=== Starting app ===' && next start -p ${PORT:-3000}"
