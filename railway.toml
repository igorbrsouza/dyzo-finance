[build]
builder = "NIXPACKS"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["nodejs_22_14", "npm-10_x", "openssl"]

[deploy]
startCommand = "npx prisma db push && node scripts/init-db.js && next start"
